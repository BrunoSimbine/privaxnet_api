services:
  txapo_api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:5000"
    networks:
      - faturize_api_network

networks:
  faturize_api_network:
    driver: bridge
      